---
layout: layouts/page.njk
permalink: /projects/index.html
title: 项目
eyebrow: Projects
description: 长文背后真实存在、持续生长的作品与实验。
---
<div class="directory-grid">
  {% for item in projects %}
  <a class="directory-entry" href="{{ item.url }}"><span class="section-label">{{ item.status }}</span><strong>{{ item.title }}</strong><span>{{ item.description }}</span><span>查看项目 →</span></a>
  {% endfor %}
</div>
